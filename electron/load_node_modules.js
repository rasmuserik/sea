// load all needed node-modules for electron here.
// (we cannot require them in sea.js, 
// as they would be inlined in dist.js in that case)
self.node_modules = {
  http: require('http'),
  ws: require('ws'),
}
